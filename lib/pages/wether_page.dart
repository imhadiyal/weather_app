import 'package:flutter/material.dart';

class WetherPage extends StatelessWidget {
  const WetherPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
